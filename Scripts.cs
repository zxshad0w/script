using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scripts : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //для выведеня в консоль
        print("Hello, World!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
